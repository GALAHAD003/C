#include <stdio.h> 

 int main() 
{
    int a, b, rows;

    printf("enter number of rows");
    scanf("%d", &rows);

    for (a = 0; a <= rows;a++)
    {
        for (b = 0; b <= rows;b++)
        {
            if ()
            printf("*");
        }
        printf("\n");
        }
        return 0;
}