#include<stdio.h>
  

int main()
{
    int length=3,breadth=4;
     int c=length*breadth;
    
    printf("the value of a is %d",  length);
    printf("the value of b is %d",breadth);
    printf("the area of ractangle is %d",c);
    return 0;
}